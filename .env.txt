
Remove-Item -Force .env
"APP_NAME=\"Smart Crop Advisory\"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

DB_CONNECTION=sqlite

SESSION_DRIVER=array
CACHE_DRIVER=array
QUEUE_CONNECTION=sync" | Out-File -FilePath .env -Encoding utf8

# Generate key
php artisan key:generate

# Start server
php artisan serve